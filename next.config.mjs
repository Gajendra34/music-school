/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'wallpapercave.com','assets.tonegym.co','www.googobits.com']
    }
};

export default nextConfig;
